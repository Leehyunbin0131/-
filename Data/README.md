# Data (local only)

Place raw statistical files here for ingestion (Excel, etc.). This folder is not tracked in git because files are often large or license-specific.

Run ingestion after adding files:

`POST /api/v1/ingestion/run`

See the main project `README` in `backend/` for API usage.
