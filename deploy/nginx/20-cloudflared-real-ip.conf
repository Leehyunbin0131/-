# cloudflared가 로컬(127.0.0.1)에서 Nginx로 넘길 때, 방문자 실제 IP를 쓰도록 함.
# (한국 geo, 로그, rate limit이 의도대로 동작)
real_ip_header CF-Connecting-IP;
real_ip_recursive on;
set_real_ip_from 127.0.0.1;
set_real_ip_from ::1;
